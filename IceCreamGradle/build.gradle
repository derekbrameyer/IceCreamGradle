buildscript {
    repositories {
        mavenCentral()
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:0.5.+'
    }
}
apply plugin: 'android'

repositories {
    mavenCentral()
}

dependencies {
    compile 'com.android.support:appcompat-v7:18.0.0'
    //compile 'com.google.code.gson:gson:2.2.2'
}

android {
    compileSdkVersion 18
    buildToolsVersion "18.1.0"
    productFlavors {
        choc {
            packageName 'com.romeo.icecreamgradle.choc'
        }
        vanill {
            packageName 'com.romeo.icecreamgradle.vanill'
        }
        strawb {
            packageName 'com.romeo.icecreamgradle.strawb'
        }
    }
    sourceSets {
        main {
            java {
                srcDir 'src/main/java'
            }
            res {
                srcDir 'src/main/res'
            }
            assets {
                srcDir 'src/mainassets'
            }
            manifest {
                srcFile 'src/main/AndroidManifest.xml'
            }
        }
        choc {
            java {
                srcDir 'src/choc/java'
            }
            res {
                srcDir 'src/choc/res'
            }
        }
        vanill {
            java {
                srcDir 'src/vanill/java'
            }
            res {
                srcDir 'src/vanill/res'
            }
        }
        strawb {
            java {
                srcDir 'src/strawb/java'
            }
            res {
                srcDir 'src/strawb/res'
            }
        }
    }
    defaultConfig {
        minSdkVersion 14
        targetSdkVersion 18
    }
    signingConfigs {
        release
    }

    buildTypes {
        debug {
            versionNameSuffix "-debug"
        }
        release {
            runProguard true
            versionNameSuffix "-release"
            zipAlign true
            signingConfig signingConfigs.release
        }
    }

    if (project.hasProperty('keyStore')){
        android.signingConfigs.release.storeFile = file(keyStore)
    }
    if (project.hasProperty('keyStorePassword')){
        android.signingConfigs.release.storePassword = keyStorePassword
    }
    if (project.hasProperty('keyStoreAlias')){
        android.signingConfigs.release.keyAlias = keyStoreAlias
    }
    if (project.hasProperty('keyStoreAliasPassword')){
        android.signingConfigs.release.keyPassword = keyStoreAliasPassword
    }
}
